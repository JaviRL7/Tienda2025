# Frontend Environment Variables
NEXT_PUBLIC_API_URL=https://your-backend-url.railway.app/api

# Backend Environment Variables (Railway auto-provides these)
DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway
SPRING_PROFILES_ACTIVE=prod
PORT=8080

# For local development with Railway DB
RAILWAY_DB_PASSWORD=your_railway_db_password_here

# Available Spring Profiles:
# - dev: Local PostgreSQL (localhost:5432)
# - local: Railway public URL (development from PC)
# - prod: Railway internal URL (production deployment)